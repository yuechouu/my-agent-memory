# my-agent-memory 开发进度 (2026-05-28)

## 已完成

### Phase 1A: Smart Tag Enhancement ✅
- `db.py`: `get_tag_frequencies()` — 标签频率统计
- `validate.py`: `validate_tags()` + `DEFAULT_TAG_BLACKLIST` — 标签格式/黑名单验证
- `llm.py`: 重写 `TAG_SUGGEST_PROMPT` — 带已有标签池 + memory_type 上下文
- `store.py`: `_schedule_tag_suggestion` 增强 — 传入标签频率 + memory_type + 验证

### Phase 1B: More Agent Tools ✅
- `providers/hermes.py`: 从 3 个 tool 扩展到 10 个
  - memory_search, memory_save, memory_pin (原有)
  - memory_recall, memory_list, memory_update, memory_archive, memory_dream, memory_conflicts, memory_tag_graph (新增)

### Phase 2A: Reranker ✅
- `reranker.py`: SiliconFlow reranker API 客户端 (BAAI/bge-reranker-v2-m3)
- `search.py`: HybridSearch 加 reranker 参数，Stage 3 精排
- `store.py`: 初始化 Reranker，`hybrid_search(rerank=True)`
- `cli.py`: `hybrid --rerank` flag

### Phase 2B: TagGraph ✅
- `schema.py`: `tag_relations` 表 (tag_a, tag_b, co_occurrence_count, similarity)
- `tag_graph.py`: `update_cooccurrence()`, `get_related_tags()`, `expand_query()`
- `store.py`: save 时更新共现矩阵
- `providers/hermes.py`: `memory_tag_graph` tool

### Phase 3A: Pi Code Extension ✅
- `extensions/pi-memory/` — TypeScript 原生扩展
  - `memory-client.ts`: HTTP API 客户端
  - `types.ts`: 共享类型定义
  - `pi-memory.ts`: 10 个 tool + 热层注入 + 3 个斜杠命令
  - `package.json`: Pi manifest

### REST API 补充 ✅
- `serve.py`: `/api/search` + `/api/tag-graph` 端点

### Memory Types ✅
- `memory_types.py`: procedural/entity/knowledge 三种类型
- per-type scoring (半衰期, promote/demote/archive 阈值)
- per-type dreaming (不衰减类型永不自动降级)
- hot layer 按类型分组 (⚙️ 流程性 / 📚 知识性 / 🏷️ 实体性)

### Dead Code Cleanup ✅
- 删除 `provider.py` + `providers/hanako.py` (未使用的 ABC 和实现)
- Hermes 插件统一到 `providers/hermes.py`

### Pi Code 事件驱动自动化 ✅ (2026-05-28 晚)
- 三层机制: Tool Description 触发条件 + System Prompt 指南 + 事件兜底
- Layer 1: 6 个 tool description 加入触发条件
- Layer 2: `MEMORY_GUIDELINES` 注入 `before_agent_start`
- Layer 3: `tool_call`(追踪搜索) + `input`(保存意图) + `context`(兜底 prefetch) + `turn_end`(自动提取) + `session_shutdown`
- 文件: `extensions/pi-memory/pi-memory.ts`

### Hermes 插件三层机制 ✅ (2026-05-28 晚)
- Layer 1: 6 个 tool schema description 加入触发条件
- Layer 2: `system_prompt_block()` 注入 `MEMORY_GUIDELINES`
- Layer 3: `on_turn_start`(保存意图+兜底 prefetch) + `handle_tool_call`(追踪搜索) + `prefetch`(返回 fallback 结果)
- 状态变量: `_agent_searched_this_turn`, `_save_flagged`, `_pending_prefetch`
- 文件: `providers/hermes.py`

### Dashboard Hermes Teal 改造 ✅ (2026-05-28 晚)
- 视觉: 深青 `#041c1c` + 暖奶油 `#ffe6cb` + 琥珀光晕 + 噪点纹理 + 8px 圆角
- 布局: 264px sidebar + backdrop-blur header
- 组件: shadcn 风格 Badge/Button/Card/Modal
- 新功能: memory_type 筛选/badge/编辑, Tag Graph Modal, By Type 统计面板
- API: `store.py` + `serve.py` 补丁 `memory_type` 参数传递
- 文件: `static/dashboard.html`, `serve.py`, `store.py`

## 待实施

### Dreaming 自动化调度
- 当前是被动触发（CLI/API/工具调用），无内置定时器
- 方案待定：外部 cron / 服务端定时器 / Agent 触发

### Phase 3 剩余
- Provider ABC (`provider.py`) — hermes-agent 侧已有 MemoryProvider ABC，可能不需要本地版本
- JS Provider HTTP 化 (`js/hermes-provider.js`)
- Claude Code 接入
- noor 数据迁移（脚本已就绪，等执行）
- hanako MemoryProvider ABC（待 hanako 确认）

## 文件结构

```
src/my_agent_memory/
├── config.py
├── db.py                 ← Database + FTS5 + sqlite-vec
├── dreaming.py           ← 生命周期引擎
├── embed.py              ← SiliconFlow embedding
├── hot_layer.py          ← Markdown 热层生成
├── llm.py                ← LLM client + prompt builders
├── memory_types.py       ← 类型配置 (procedural/entity/knowledge)
├── migrate.py            ← v1→v2 迁移
├── provider.py           ← DELETED (unused ABC)
├── reranker.py           ← SiliconFlow reranker
├── schema.py             ← SQL DDL + triggers
├── scoring.py            ← score = log2(access) × decay × weight
├── search.py             ← FTS5 + vector + RRF + reranker
├── serve.py              ← HTTP REST API server
├── store.py              ← MultiAgentStore (central API)
├── tag_graph.py          ← 标签共现图谱
├── validate.py           ← 双层注入检测 + 标签验证
├── cli.py                ← 20+ CLI 子命令
├── extensions/
│   └── pi-memory/        ← Pi Code 原生扩展
│       ├── pi-memory.ts
│       ├── memory-client.ts
│       ├── types.ts
│       └── package.json
└── providers/
    ├── __init__.py
    └── hermes.py          ← Hermes 插件 (10 tools)
```

## 测试
- 75+ tests passing
- test_scoring, test_validate, test_search_rrf, test_hot_layer, test_integration, test_memory_types, test_tag_enhancement, test_tag_graph

## Commits
- `6f70677` refactor: remove dead code, unify Hermes plugin
- `ccca283` feat: VCP-inspired improvements — smart tags, tools, reranker, tag graph, Pi extension
